meta {
    title: "Street lamps";
    description: "Highlights highway=street_lamp objects with different colors depending on the lamp type (LED and electric). Inspired by the OSMStreetLight project. Based on and recommended for use with the LitObjects style.";
    author: "NieWnen";
    version: "1.2_2025-10-13";
}

node[highway=street_lamp]::*{ prop_lamp_type: "electric"; prop_symbol_fill_color: "#ffbb44"; }
node[lamp_type=LED]::*{ prop_lamp_type: "led"; prop_symbol_fill_color: "#faf9f0"; }
node[lamp_type=led]::*{ prop_lamp_type: "led"; prop_symbol_fill_color: "#faf9f0"; }

/* Base glow */
node[highway=street_lamp]::lighting_glows {
    object-z-index: -1.0;
    symbol-shape: circle;
    symbol-size: 40;
    symbol-fill-opacity: 0.5;
    symbol-fill-color: prop(prop_symbol_fill_color);
}

/* Light points with light:count/light:direction tags */

/*
Each lamp point contains invisible text, because lamp points tend to glitch slightly.
For example, when the direction value changes, the lamp point start to jump when selecting object or zooming the view.
The "text" label forces JOSM to re-render on each degree change.

I suppose that JOSM caches certain attribute values, such as "text", but not "icon-offset-*",  and it listens for any changes in those cache attributes to trigger a re-render.
*/

/* --- Light point 1 --- */
node[highway=street_lamp][light:count>1]::light_1 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 0;
    degree: cond(prop(direction_len) > 0, get(prop(direction_list), 0), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 2 --- */
node[highway=street_lamp][light:count>1]::light_2 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 360/tag("light:count");
    degree: cond(prop(direction_len) > 1, get(prop(direction_list), 1), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 3 --- */
node[highway=street_lamp][light:count>2]::light_3 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 2*360/tag("light:count");
    degree: cond(prop(direction_len) > 2, get(prop(direction_list), 2), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 4 --- */
node[highway=street_lamp][light:count>3]::light_4 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 3*360/tag("light:count");
    degree: cond(prop(direction_len) > 3, get(prop(direction_list), 3), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 5 --- */
node[highway=street_lamp][light:count>4]::light_5 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 4*360/tag("light:count");
    degree: cond(prop(direction_len) > 4, get(prop(direction_list), 4), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 6 --- */
node[highway=street_lamp][light:count>5]::light_6 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 5*360/tag("light:count");
    degree: cond(prop(direction_len) > 5, get(prop(direction_list), 5), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 7 --- */
node[highway=street_lamp][light:count>6]::light_7 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 6*360/tag("light:count");
    degree: cond(prop(direction_len) > 6, get(prop(direction_list), 6), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

/* --- Light point 8 --- */
node[highway=street_lamp][light:count>7]::light_8 {
    direction_list: split(";", tag("light:direction"));
    direction_len: length(prop(direction_list));
    
    default_degree: 7*360/tag("light:count");
    degree: cond(prop(direction_len) > 7, get(prop(direction_list), 7), prop(default_degree));
    x: round(sin(degree_to_radians(prop(degree))) * 15);
    y: round(-cos(degree_to_radians(prop(degree))) * 15);
    text: concat(prop(x), ", ", prop(y));
    text-opacity: 0.0;
    
    icon-width: 80;
    icon-height: 80;
    icon-opacity: 0.9;
    icon-offset-x: prop(x);
    icon-offset-y: prop(y);
}

node[prop("prop_lamp_type")="led"]::light_1 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_2 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_3 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_4 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_5 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_6 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_7 { icon-image: "icons/electric_led.svg" }
node[prop("prop_lamp_type")="led"]::light_8 { icon-image: "icons/electric_led.svg" }

node[prop("prop_lamp_type")!="led"]::light_1 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_2 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_3 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_4 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_5 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_6 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_7 { icon-image: "icons/electric.svg" }
node[prop("prop_lamp_type")!="led"]::light_8 { icon-image: "icons/electric.svg" }

